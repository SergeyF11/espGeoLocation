# ESP GeoLocation Library for ESP8266/ESP32

## Описание

**GeoLocation** — это легковесная библиотека для ESP8266 и ESP32, которая автоматически определяет геопозицию, временную зону и другую географическую информацию на основе IP-адреса устройства. Библиотека получает данные с бесплатного геолокационного сервиса ip-api.com и позволяет автоматически настроить системное время и часовой пояс с дальнейшей синхронизацией через NTP.
Данный сервис не требует получение ключа для частного использования.

## Основные возможности

- 🌍 **Автоматическое определение местоположения** по IP-адресу
- 🕐 **Определение временной зоны** и смещения времени (UTC offset)
- 📍 **Получение географических данных**: страна, город, координаты (широта/долгота)
- ⏰ **Синхронизация времени** с NTP-серверами в соответствующем регионе
- 🔧 **Простая настройка** системного времени через обёртку для `configTime()`
- 🔧 **Блокирующий и асинхронный режимы** для универсального использования

## Быстрый старт

### Установка

1. **Установка через Arduino IDE:**
   - Скачайте архив библиотеки
   - В Arduino IDE: Скетч → Подключить библиотеку → Добавить .ZIP библиотеку...

2. **Ручная установка:**
   - Клонируйте репозиторий в папку libraries вашей Arduino IDE: `~/Arduino/libraries/GeoLocation/`

3. **Примеры**
   - См. паку examples библиотеки

4. **API Reference**
### Основные методы

| Метод | Описание | Возвращаемое значение |
|-------|----------|----------------------|
| `GeoLocation::GeoLocation geoService;` | Создание экземпляра сервиса | `GeoLocation` |
| `cconfigTime( NTP_SERVER1, NTP_SERVER2, NTP_SERVER3 );` | Конфигурирование NTP-серверов | `void` |
| `getLocation(bool autoSetTime, const char* lang, unsigned long timeout);` | Получить данные и установить (опционально) системное время  | `bool` |
| `getResult()` | Получить данные | `GeoLocation::GeoData` |
| `getResult().printTo(Serial)` | Вывести результат в Serial | `void` |

......


**Версия**
Текущая версия: 0.0.1

**Автор**
Sergey Fedotov (SergeyF11)
https://github.com/SergeyF11
